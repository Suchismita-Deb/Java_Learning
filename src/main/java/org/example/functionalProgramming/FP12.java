package org.example.functionalProgramming;

public class FP12 {
    public static void main(String[] args) {
        // In stream we mainly have two type of operations.
        // Intermediate and Teminal Operation.

        // When clicking on the method name then we can see that the return type is stream.
        // They are the Intermediate operation. (distinct, sorted, map, filter).
        // Reduce, Collect all are the terminal operation.
        // Terminal operations return other than stream like void, list.
        // Return of reduce is one element like sum.
    }
}
